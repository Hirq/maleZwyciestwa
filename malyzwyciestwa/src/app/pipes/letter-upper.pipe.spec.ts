import { LetterUpperPipe } from './letter-upper.pipe';

describe('LetterUpperPipe', () => {
  it('create an instance', () => {
    const pipe = new LetterUpperPipe();
    expect(pipe).toBeTruthy();
  });
});
